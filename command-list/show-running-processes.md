# Show running processes



```
top    -> shows all the processes that are running on the computer interactively. That is, the list of processes is updated

ps -wA -> shows all processes that are running and where they come from. This command is not interactive
```
