# SSH login



```
ssh user@host           -> connects to host as user
ssh host                -> securely connects to host via SSH default port 22
ssh -p [port] user@host -> connects to host using a particular port
telnet host             -> connects to host via telnet default port 23
```
