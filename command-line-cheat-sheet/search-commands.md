# Search commands



```
find <path> -name <name>   -> searches into a path with a file name
find. -name <file>         -> finds a file in current directory
grep <regex> <file>        -> searches a regular expressions inside a file or terminal output
grep -i [word] message.txt -> looks for a word ignoring lowercase and uppercase
```
