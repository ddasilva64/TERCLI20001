# File transfer



```
scp [file_name.txt] [server/tmp]      -> securely copies a specific file to a server directory
rsync -a [/your/directory] [/backup/] -> synchronizes the contents of a specific directory with a backup directory
```
