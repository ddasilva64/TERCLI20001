# Environment variables



```
env               -> prints current environment variables
echo $VAR         -> prints a environment variable
$ PATH            -> variable in which are executables paths
$ HOME            -> user's home directory
export $VAR = val -> assigns a variable with a value
```
